# Show recorded steps

Prints a numbered list of all steps in the session, with flagged steps
highlighted.

## Usage

``` r
show_steps(sess)
```

## Arguments

- sess:

  A `macrox_session` object.

## Value

`sess` invisibly.
