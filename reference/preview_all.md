# Preview all extracted tables with numeric totals

Preview all extracted tables with numeric totals

## Usage

``` r
preview_all(sess, n = 6)
```

## Arguments

- sess:

  A `macrox_session` object.

- n:

  Number of rows to show per table (default 6).

## Value

`sess` invisibly. \# not recorded
