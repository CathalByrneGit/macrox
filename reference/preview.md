# Preview one extracted table

Preview one extracted table

## Usage

``` r
preview(sess, table, n = 6)
```

## Arguments

- sess:

  A `macrox_session` object.

- table:

  Character label of the table to preview.

- n:

  Number of rows to show (default 6).

## Value

`sess` invisibly. \# not recorded
