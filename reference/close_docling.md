# Release the Docling document cache

Frees the in-memory cache of converted PDF documents. Call this when you
are done extracting tables to reduce memory usage.

## Usage

``` r
close_docling()
```

## Value

Invisible `NULL`.
