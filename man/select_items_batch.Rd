% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/items.R
\name{select_items_batch}
\alias{select_items_batch}
\title{Extract multiple metadata fields in one GLiNER model pass}
\usage{
select_items_batch(
  sess,
  items,
  page = NULL,
  cast = NULL,
  gliner_model = "fastino/gliner2-base-v1",
  all_matches = FALSE
)
}
\arguments{
\item{sess}{A \code{macrox_session} object.}

\item{items}{Named character vector: names are field labels, values are
natural-language prompts describing the field.
E.g. \code{c(invoice_no = "Invoice ID or reference number", total = "Grand total amount payable")}.}

\item{page}{Page number to extract from.  \code{NULL} (default) concatenates all
pages' text.}

\item{cast}{Named character vector of cast types keyed by label.  Unlisted
fields default to \code{"character"}.  Supported types: \code{"character"},
\code{"numeric"}, \code{"integer"}, \code{"date:<fmt>"} (e.g. \code{"date:\%d/\%m/\%Y"}).}

\item{gliner_model}{GLiNER2 model identifier.  Default
\code{"fastino/gliner2-base-v1"} (205M); use \code{"fastino/gliner2-large-v1"} for
higher accuracy.}

\item{all_matches}{If \code{TRUE}, return every occurrence found per label as a
character vector.  Labels listed in \code{cast} are still applied element-wise.
Default \code{FALSE}.}
}
\value{
\code{sess} invisibly (one \code{select_item} step recorded per field).
}
\description{
Runs a single GLiNER2 inference call to extract several named fields from a
PDF, which is significantly faster than calling \code{\link[=select_item]{select_item()}} once per
field. Each field is recorded as a separate \code{select_item} step so macros
replay with the standard \code{select_item} mechanism.
}
\details{
GLiNER2 can return multiple occurrences of the same entity type when they
are present in the text; \code{select_items_batch} surfaces the first match per
field (the same behaviour as \code{\link[=select_item]{select_item()}}).
}
