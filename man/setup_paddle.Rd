% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/paddle.R
\name{setup_paddle}
\alias{setup_paddle}
\title{Install PaddleOCR into a Python environment}
\usage{
setup_paddle(
  envname = "r-pdfmacro",
  backend = c("onnxruntime", "paddle"),
  cuda_version = NULL,
  index_url = NULL,
  proxy = NULL,
  pip_options = character(0),
  wheels_dir = NULL
)
}
\arguments{
\item{envname}{Name of the Python environment (default \code{"r-pdfmacro"}).}

\item{backend}{Inference backend: \code{"onnxruntime"} (default, CPU-only,
lightweight) or \code{"paddle"} (supports GPU via \code{cuda_version}).}

\item{cuda_version}{CUDA version string for GPU builds, e.g. \code{"12.6"}.
Only used when \code{backend = "paddle"}. Supported: \code{"11.8"}, \code{"12.0"},
\code{"12.3"}, \code{"12.4"}, \code{"12.6"}.}

\item{index_url}{Custom PyPI index URL for restricted networks, e.g.
\code{"https://packagemanager.posit.co/pypi/latest/simple"}.}

\item{proxy}{HTTP/HTTPS proxy URL, e.g. \code{"http://proxy.corp:8080"}.}

\item{pip_options}{Extra pip flags, e.g.
\code{c("--trusted-host", "pypi.internal.corp")}.}

\item{wheels_dir}{Path to a local directory of pre-downloaded \code{.whl} files
for fully offline install. Use \code{\link[=download_paddle_wheels]{download_paddle_wheels()}} to prepare it.}
}
\value{
Invisible \code{NULL}.
}
\description{
Creates (or reuses) a conda/virtualenv environment and installs
PaddleOCR and a suitable inference backend.
Run once before using \code{select_table_paddle()}, then restart R.
}
\details{
\subsection{Choosing a backend}{

PaddleOCR 3.x supports multiple inference backends. The default is
\code{backend = "onnxruntime"} which is strongly recommended for restricted
environments:
\itemize{
\item Only ~20 MB (vs ~200 MB for PaddlePaddle).
\item Ships on standard PyPI — internal mirrors almost always carry it.
\item No \code{paddlepaddle} package needed at all.
\item Models are auto-converted to ONNX on first use (internet required once).
}

Use \code{backend = "paddle"} if you need GPU inference or are installing from
offline wheels that include paddlepaddle.
}
}
\examples{
\dontrun{
# Recommended for restricted networks — only needs onnxruntime from PyPI
setup_paddle()

# Internal mirror (onnxruntime is almost always available there)
setup_paddle(index_url = "https://packagemanager.posit.co/pypi/latest/simple")

# GPU via PaddlePaddle backend — CUDA 12.6
setup_paddle(backend = "paddle", cuda_version = "12.6")

# Offline: download wheels first (on an internet machine), then:
download_paddle_wheels("~/paddleocr_wheels")
setup_paddle(wheels_dir = "/path/on/server/paddleocr_wheels")
}
}
