% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/detect.R
\name{detect_tables}
\alias{detect_tables}
\title{Scan a PDF for tables}
\usage{
detect_tables(
  sess,
  pages = NULL,
  method = c("lattice", "stream", "docling"),
  min_rows = 1L,
  max_header_chars = 40L
)
}
\arguments{
\item{sess}{A \code{macrox_session} object.}

\item{pages}{Integer vector of pages to scan. Defaults to all pages.}

\item{method}{Extraction method: \code{"lattice"} (default), \code{"stream"}, or
\code{"docling"}. Docling converts all requested pages in one pass and is
best for scanned PDFs or complex layouts.}

\item{min_rows}{Minimum number of data rows a table must have to be shown
and stored. Default \code{1}. Set to \code{0} to see everything.}

\item{max_header_chars}{Maximum character length allowed for the first
column name before the table is flagged as a likely chart/figure and
skipped. Default \code{40}. Set to \code{Inf} to disable. (Lattice/stream only —
Docling's layout model distinguishes charts from tables natively.)}
}
\value{
\code{sess} invisibly (not recorded).
}
\description{
Scans the specified pages of the PDF for tables and prints a summary.
This function is NOT recorded in the macro — it is for exploration only.
}
\details{
Results are stored in \code{sess$detect} as a list of
\code{list(page, index, df)} entries that can be passed directly to
\code{\link[=select_table]{select_table()}} via \code{table_index}. When \code{method = "docling"}, pages are
converted in a single pass and the results are cached so that subsequent
\code{\link[=select_table_docling]{select_table_docling()}} calls on those pages run instantly.
}
