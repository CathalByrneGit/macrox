% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/detect.R
\name{detect_tables}
\alias{detect_tables}
\title{Scan a PDF for tables}
\usage{
detect_tables(
  sess,
  pages = NULL,
  method = "lattice",
  min_rows = 1L,
  max_header_chars = 40L
)
}
\arguments{
\item{sess}{A \code{pdfmacro_session} object.}

\item{pages}{Integer vector of pages to scan. Defaults to all pages.}

\item{method}{Extraction method: \code{"lattice"} (default) or \code{"stream"}.}

\item{min_rows}{Minimum number of data rows a table must have to be shown
and stored. Tables with fewer rows are silently skipped. Default \code{1}
(i.e. 0-row tables — header-only extractions — are dropped). Set to \code{0}
to see everything.}

\item{max_header_chars}{Maximum character length allowed for the first column
name before the table is flagged as a likely chart/figure and skipped.
Long first-column names like
\code{"X800.000.700.000.600.000..."} are a reliable signal that tabulapdf has
picked up a bar/line chart axis rather than a real table. Default \code{40}.
Set to \code{Inf} to disable.}
}
\value{
\code{sess} invisibly. # not recorded
}
\description{
Scans the specified pages of the PDF for tables and prints a summary.
This function is NOT recorded in the macro — it is for exploration only.
}
