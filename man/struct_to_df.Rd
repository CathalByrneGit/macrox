% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/items.R
\name{struct_to_df}
\alias{struct_to_df}
\title{Convert raw struct extraction to a data frame}
\usage{
struct_to_df(sess, label, min_confidence = NULL)
}
\arguments{
\item{sess}{A \code{macrox_session} object.}

\item{label}{Label matching a prior \code{\link[=select_struct]{select_struct()}} call.}

\item{min_confidence}{Numeric in \verb{[0, 1]}. Records whose mean field
confidence is below this threshold are dropped. \code{NULL} (default) keeps all.}
}
\value{
\code{sess} invisibly (step recorded).
}
\description{
Takes the raw GLiNER2 records stored by \code{\link[=select_struct]{select_struct()}} (which include
per-field confidence scores) and converts them to a data frame stored in
\code{sess$tables[[label]]}. Optionally filters out low-confidence records.
}
