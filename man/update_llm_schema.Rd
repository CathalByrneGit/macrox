% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/llm.R
\name{update_llm_schema}
\alias{update_llm_schema}
\title{Update the schema for a recorded LLM extraction step and re-extract}
\usage{
update_llm_schema(
  sess,
  label,
  schema,
  prompt = NULL,
  chat = NULL,
  re_extract = TRUE
)
}
\arguments{
\item{sess}{A \code{macrox_session} object.}

\item{label}{Label of the table to update.}

\item{schema}{New named character vector: \code{c(Month = "character", Male = "integer")}.}

\item{prompt}{Replacement prompt, or \code{NULL} to keep the existing one.}

\item{chat}{An existing \code{ellmer} Chat object to use for the re-extraction.
See \code{\link[=select_table_llm]{select_table_llm()}}. \code{NULL} (default) reuses the provider/model
recorded for the step.}

\item{re_extract}{Re-run the LLM call immediately (default \code{TRUE}).}
}
\value{
\code{sess} invisibly.
}
\description{
Finds the most recent \code{select_table_llm} step for \code{label}, replaces its
schema, and re-runs the extraction against the same page/area.  Useful
when the auto-detected columns need renaming or the types need adjustment.
}
