% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transform.R
\name{clean_numbers}
\alias{clean_numbers}
\title{Clean and parse numeric strings in table columns}
\usage{
clean_numbers(
  sess,
  table,
  cols = NULL,
  currency = c("£", "$", "€", "¥"),
  na_strings = c("-", "—", "n/a", "na", ""),
  negative_parens = TRUE,
  convert = TRUE
)
}
\arguments{
\item{sess}{A \code{macrox_session} object.}

\item{table}{Character label of the target table.}

\item{cols}{Character vector of column names to process. \code{NULL} (default)
targets all non-numeric columns.}

\item{currency}{Character vector of currency symbols to strip.}

\item{na_strings}{Strings that should become \code{NA} (case-insensitive after
whitespace trimming).}

\item{negative_parens}{If \code{TRUE} (default), converts \code{(123)} to \code{-123}.}

\item{convert}{If \code{TRUE} (default), coerces a column to \code{numeric} when all
non-NA values parse successfully.}
}
\value{
\code{sess} invisibly (step is recorded).
}
\description{
Strips thousands separators, currency symbols, and parenthetical negatives;
maps sentinel strings to \code{NA}; optionally coerces columns to numeric.
}
