% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/docling.R
\name{select_table_docling}
\alias{select_table_docling}
\title{Extract a table using Docling}
\usage{
select_table_docling(sess, label, page, table_index = 1L)
}
\arguments{
\item{sess}{A \code{macrox_session} object.}

\item{label}{Character label for the extracted table.}

\item{page}{Page number (integer).}

\item{table_index}{Which table to use when multiple are detected on the
page (default 1).}
}
\value{
\code{sess} invisibly (step is recorded).
}
\description{
Converts the PDF page using Docling's ML layout pipeline and returns a
clean data frame. Works fully offline after the initial one-time model
download; handles scanned PDFs and complex layouts where \code{bbox} fails.
}
\details{
Docling converts the target page the first time it is called and caches
the result in the Python session, so repeated calls for different tables
on the same page do not re-run the conversion pipeline. Call
\code{\link[=close_docling]{close_docling()}} to release the cache when done.
}
