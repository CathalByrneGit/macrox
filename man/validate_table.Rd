% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate_table.R
\name{validate_table}
\alias{validate_table}
\title{Validate an extracted table against a set of rules}
\usage{
validate_table(sess, table, rules, strict = FALSE)
}
\arguments{
\item{sess}{A \code{pdfmacro_session} object.}

\item{table}{Character label of the table to validate.}

\item{rules}{Named character vector or named list of R expression strings.
Names become the rule identifiers shown in reports. Alternatively, a
\code{validate::validator()} object.}

\item{strict}{If \code{TRUE}, any rule failure calls \code{cli_abort()} instead of
\code{cli_warn()}. Default \code{FALSE}.}
}
\value{
\code{sess} invisibly (step is recorded).
}
\description{
Uses the \code{validate} package DSL. Rules are named character strings
containing R expressions evaluated against the data frame. Both row-level
predicates (\code{total_count > 0}) and table-level aggregates (\code{nrow(.) == 12})
are supported.
}
\details{
Results are stored in \code{sess$validations[[table]]} for later inspection.
On replay, failures produce warnings by default; set \code{strict = TRUE} to
abort.
}
\examples{
\dontrun{
sess |> validate_table("calf_monthly", rules = c(
  no_na_month    = "!anyNA(month)",
  positive_males = "all(male_count > 0, na.rm = TRUE)",
  twelve_rows    = "nrow(.) == 12"
))
}
}
