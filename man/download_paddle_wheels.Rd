% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/paddle.R
\name{download_paddle_wheels}
\alias{download_paddle_wheels}
\title{Generate a pip download command for offline PaddleOCR installation}
\usage{
download_paddle_wheels(
  dest,
  python_version = "3.9",
  platform = "manylinux_2_17_x86_64",
  cuda_version = NULL,
  run = FALSE
)
}
\arguments{
\item{dest}{Directory to download wheels into (created if absent).}

\item{python_version}{Python version on the \strong{target server}, e.g.
\code{"3.9"} (default). Match this to what Posit Workbench uses.}

\item{platform}{Platform tag for the target server, e.g.
\code{"manylinux_2_17_x86_64"} (default — Linux x86_64).
Use \code{"win_amd64"} for Windows or \code{"macosx_12_0_x86_64"} for Intel Mac.}

\item{cuda_version}{CUDA version string for GPU wheels, e.g. \code{"12.6"}.
\code{NULL} (default) downloads CPU wheels only.}

\item{run}{Logical; actually run the command via \code{system()} (default \code{FALSE}).
When \code{FALSE}, just prints the command for you to run manually.}
}
\value{
The pip command string, invisibly.
}
\description{
Prints (and optionally runs) the \verb{pip download} command needed to fetch all
PaddleOCR wheels for your server's platform and Python version. Run this on
an \strong{internet-connected} machine, copy the resulting folder to your server,
then call \code{setup_paddle(wheels_dir = "/path/to/folder")}.
}
