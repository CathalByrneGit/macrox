% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/paddle.R
\name{select_table_paddle}
\alias{select_table_paddle}
\title{Extract a table using PP-StructureV3 (PaddleOCR, offline)}
\usage{
select_table_paddle(
  sess,
  label,
  page,
  area = NULL,
  dpi = 150L,
  header_rows = 1L,
  table_index = 1L,
  device = c("cpu", "gpu"),
  backend = c("auto", "onnxruntime", "paddle"),
  debug = FALSE
)
}
\arguments{
\item{sess}{A \code{pdfmacro_session} object.}

\item{label}{Character label for the extracted table.}

\item{page}{Page number (integer).}

\item{area}{Named \code{c(top, left, bottom, right)} in PDF points. When
supplied the image is cropped before being sent — faster and more accurate.}

\item{dpi}{Render resolution (default 150).}

\item{header_rows}{Number of header rows (default 1).}

\item{table_index}{Which table to use when multiple are detected (default 1).}

\item{device}{\code{"cpu"} (default) or \code{"gpu"}. GPU requires a GPU build of
PaddlePaddle installed via \code{\link[=setup_paddle]{setup_paddle()}}.}

\item{backend}{Inference backend: \code{"auto"} (default — uses whatever is
installed), \code{"onnxruntime"}, or \code{"paddle"}. Override when both backends
are installed and you want to force a specific one.}
}
\value{
\code{sess} invisibly (step is recorded).
}
\description{
Renders the PDF page (optionally cropped to \code{area}), passes the image to
PP-StructureV3, and returns a clean data frame. Works fully offline — no
API keys required. Ideal for scanned PDFs and complex layouts where \code{bbox}
fails.
}
