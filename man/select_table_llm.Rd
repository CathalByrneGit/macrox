% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/llm.R
\name{select_table_llm}
\alias{select_table_llm}
\title{Extract a table using an LLM}
\usage{
select_table_llm(
  sess,
  label,
  page,
  area = NULL,
  chat = NULL,
  provider = "anthropic",
  model = NULL,
  base_url = NULL,
  schema = NULL,
  prompt = NULL,
  dpi = 150L,
  header_rows = 1L
)
}
\arguments{
\item{sess}{A \code{pdfmacro_session} object.}

\item{label}{Character label for the extracted table.}

\item{page}{Page number (integer).}

\item{area}{Named numeric vector \code{c(top, left, bottom, right)} in PDF
points.  When supplied the image sent to the LLM is cropped to that
region, which improves accuracy on pages with multiple tables.}

\item{chat}{An existing \code{ellmer} Chat object, e.g.
\code{ellmer::chat_anthropic()}, \code{ellmer::chat_openrouter()}, or
\code{ellmer::chat_openai_compatible(base_url = "http://localhost:11434/v1", model = "llama2")}.  The chat is cloned before use (its history is left
untouched).  When supplied, \code{provider}, \code{model}, and \code{base_url} are
ignored for the call but recorded (derived from the chat object) so the
step can be replayed.}

\item{provider}{Name of an \code{ellmer} chat constructor, without the \code{chat_}
prefix, e.g. \code{"anthropic"} (default), \code{"openai"}, \code{"google_gemini"},
\code{"openrouter"}, \code{"groq"}, \code{"ollama"}, or \code{"openai_compatible"}. Any
provider for which \code{ellmer} exports \verb{chat_<provider>()} is supported.
Ignored when \code{chat} is supplied.}

\item{model}{Model name.  \code{NULL} uses a sensible default for \code{"anthropic"},
\code{"openai"}, and \code{"google_gemini"}; other providers require \code{model} to be
set explicitly.  Ignored when \code{chat} is supplied.}

\item{schema}{Named character vector mapping column names to R types:
\code{c(Month = "character", Male = "integer", Total = "integer")}.
Supported types: \code{"character"}, \code{"integer"}, \code{"numeric"}.
\code{NULL} (default) asks the LLM to auto-detect the columns.}

\item{prompt}{Optional extra instructions appended to the base prompt,
e.g. \code{"Ignore the footnote row at the bottom."}.}

\item{dpi}{Render resolution for the page image (default 150).}

\item{header_rows}{Number of header rows.  When > 1 the prompt asks the
LLM to flatten them with \verb{_} separators.}
}
\value{
\code{sess} invisibly (step is recorded).
}
\description{
Renders the PDF page (optionally cropped to \code{area}), sends it to an LLM
with a structured schema, and returns a clean data frame.  Good for
multi-level headers, tables embedded in mixed-content pages, and any
situation where positional extraction fails.
}
\details{
Requires the \code{ellmer} package (\code{install.packages("ellmer")}) and an API
key for the chosen provider set as an environment variable
(\code{ANTHROPIC_API_KEY}, \code{OPENAI_API_KEY}, \code{GEMINI_API_KEY}).
}
