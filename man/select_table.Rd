% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/select.R
\name{select_table}
\alias{select_table}
\title{Extract a table from the PDF and record the step}
\usage{
select_table(
  sess,
  label,
  page = NULL,
  table_index = 1,
  area = NULL,
  label_match = NULL,
  method = c("lattice", "stream", "bbox"),
  header_rows = 1,
  fuzzy_method = "jw",
  max_dist = 0.2,
  row_tol = NULL,
  col_gap = NULL
)
}
\arguments{
\item{sess}{A \code{macrox_session} object.}

\item{label}{Character label for the extracted table.}

\item{page}{Page number (integer). Required unless \code{label_match} is supplied.}

\item{table_index}{Which table on the page (1-based, tabulapdf methods only).
Default 1.}

\item{area}{Named numeric vector \code{c(top, left, bottom, right)} in PDF points.
Use \code{\link[=locate_area]{locate_area()}} to capture interactively. Strongly recommended with
\code{method = "bbox"} to isolate the table from surrounding charts/text.}

\item{label_match}{Text caption to fuzzy-search across all pages.}

\item{method}{Extraction engine:
\itemize{
\item \code{"lattice"} (default) — tabulapdf, best for grid-lined tables.
\item \code{"stream"} — tabulapdf, best for whitespace-separated tables.
\item \code{"bbox"} — \code{pdftools::pdf_data()} word-position engine. Handles PDFs
where tabulapdf fails: charts on the same page, missing grid lines,
numbers running together. Use with \code{area} to clip to the table region.
}}

\item{header_rows}{Number of header rows to merge (default 1). Use 2+ for
multi-row spanning headers.}

\item{fuzzy_method}{stringdist method for caption matching (default \code{"jw"}).}

\item{max_dist}{Maximum normalised distance for a fuzzy match (default 0.2).}

\item{row_tol}{\code{bbox} method only. Vertical gap (pts) above which a new row
is started. Default \code{NULL} auto-computes from median character height.}

\item{col_gap}{\code{bbox} method only. Horizontal gap (pts) between word groups
that signals a new column. Default \code{NULL} auto-computes from median word
width.}
}
\value{
\code{sess} invisibly (step is recorded).
}
\description{
Three selection modes: by page + index, by bounding box (\code{area}), or by
fuzzy caption match (\code{label_match}). These can be mixed across different
tables in the same session.
}
