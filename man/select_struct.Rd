% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/items.R
\name{select_struct}
\alias{select_struct}
\title{Extract structured records from a PDF page using GLiNER2}
\usage{
select_struct(
  sess,
  label,
  entity = label,
  fields,
  list_fields = character(0),
  enum_fields = character(0),
  page = 1L,
  gliner_model = "fastino/gliner2-base-v1"
)
}
\arguments{
\item{sess}{A \code{macrox_session} object.}

\item{label}{Table label to store results under.}

\item{entity}{Entity type name used in the GLiNER2 schema. Defaults to
\code{label}.}

\item{fields}{Named character vector: \code{field_name = "description"}. Every
name becomes a column in the resulting data frame.}

\item{list_fields}{Character vector of field names that may contain multiple
values (GLiNER2 \code{list} type). Multiple values are collapsed with \code{"; "}.
Default \code{character(0)}.}

\item{enum_fields}{Named character vector of enumerated field types:
\code{field_name = "[val1|val2|val3]"}. Restricts what the model extracts to the
listed choices. Default \code{character(0)}.}

\item{page}{Integer or integer vector of pages to extract from. Multiple
pages use a single \code{batch_extract_json} call. Default \code{1L}.}

\item{gliner_model}{GLiNER2 model identifier.
Default \code{"fastino/gliner2-base-v1"}.}
}
\value{
\code{sess} invisibly (step recorded).
}
\description{
Uses GLiNER2's \code{extract_json} to extract a structured entity schema
(multiple fields, optional list fields, multiple records) from a PDF page.
Results are stored as a data frame in \code{sess$tables[[label]]}, identical to
tabulapdf / LLM table extraction — downstream steps and exports work
unchanged.
}
