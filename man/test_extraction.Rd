% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/agent.R
\name{test_extraction}
\alias{test_extraction}
\title{Test extraction parameters and return a structured evaluation report}
\usage{
test_extraction(
  file,
  page,
  area = NULL,
  method = c("bbox", "lattice", "stream", "paddle", "llm"),
  header_rows = 1L,
  label = "target",
  provider = "anthropic",
  schema = NULL,
  preview_rows = 3L,
  ...
)
}
\arguments{
\item{file}{Path to a PDF file.}

\item{page}{Page number (integer).}

\item{area}{Named \code{c(top, left, bottom, right)} in PDF points, or \code{NULL}
for the full page.}

\item{method}{Extraction method: \code{"bbox"} (default), \code{"lattice"},
\code{"stream"}, \code{"paddle"}, or \code{"llm"}.}

\item{header_rows}{Number of header rows (default 1).}

\item{label}{Internal table label used during extraction (default
\code{"target"}).}

\item{provider}{LLM provider, only used when \code{method = "llm"}.}

\item{schema}{Named character vector of column types, only used when
\code{method = "llm"}.}

\item{preview_rows}{Number of data rows to include in the response preview
(default 3). Keeps the agent context window small.}

\item{...}{Additional arguments forwarded to the extraction function.}
}
\value{
A list with:
\describe{
\item{\code{status}}{\code{"success"}, \code{"needs_alignment"}, \code{"empty"}, or
\code{"crash"}.}
\item{\code{metrics}}{Structural quality metrics: row/col counts, unnamed
headers, empty columns, header-spill detection, likely-numeric
character columns.}
\item{\code{guidance}}{Plain-English action string for the agent when status
is not \code{"success"}.}
\item{\code{preview}}{First \code{preview_rows} rows as a list of named lists for
JSON serialisation.}
}
}
\description{
The R-Core Judge for the MCP agent loop. Runs a single extraction pass with
the given parameters and returns a structured report indicating whether the
result is clean or needs adjustment. The MCP \code{test_parameters} tool calls
this in a loop until \code{status == "success"}.
}
\details{
Returns a list that serialises cleanly to JSON via \code{jsonlite::toJSON()}.
}
